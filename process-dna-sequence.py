from argparse import ArgumentParser

# Set up arguments
parser = ArgumentParser()
parser.add_argument(
    '-',
    '--',
    type=,
    choices=[],
    default=,
    help='')

# Parse arguments
args = parser.parse_args()
